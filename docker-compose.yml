services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qubit_cpu
    working_dir: /app
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    command: bash