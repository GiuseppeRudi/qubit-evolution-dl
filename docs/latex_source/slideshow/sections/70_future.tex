\begin{frame}{Limitations \& Future Work}
\vspace{0.4em}

\begin{columns}[c,onlytextwidth]

  % ---- Left column: Today + Limit ----
  \column{0.43\textwidth}

  \begin{tcolorbox}[
    colback=gray!12,
    colframe=gray!50,
    fonttitle=\small\bfseries,
    title={Today},
    arc=5pt, boxrule=0.7pt,
    left=5pt, right=5pt, top=3pt, bottom=4pt
  ]
  {\small
  \begin{itemize}[leftmargin=1.2em, itemsep=4pt, topsep=2pt, label=\textbullet]
    \item step-wise Scheduled Sampling (SS)
  \end{itemize}
  }
  \end{tcolorbox}

  \vspace{0.7em}

  \begin{tcolorbox}[
    colback=orange!10,
    colframe=orange!65,
    fonttitle=\small\bfseries,
    title={Limitation},
    arc=5pt, boxrule=0.7pt,
    left=5pt, right=5pt, top=3pt, bottom=4pt
  ]
  {\small
  \begin{itemize}[leftmargin=1.2em, itemsep=4pt, topsep=2pt, label=\textbullet]
    \item limited parallelism $\Rightarrow$ slow for long horizons
  \end{itemize}
  }
  \end{tcolorbox}

  % ---- Middle arrow ----
  \column{0.07\textwidth}
  \centering{\large $\longrightarrow$}

  % ---- Right column: Future Work ----
  \column{0.50\textwidth}

  \begin{tcolorbox}[
    colback=blue!7,
    colframe=blue!45,
    fonttitle=\small\bfseries,
    title={Future Work},
    arc=5pt, boxrule=0.7pt,
    left=5pt, right=5pt, top=3pt, bottom=5pt
  ]
  {\small
  \begin{itemize}[leftmargin=1.2em, itemsep=4pt, topsep=2pt, label=\textbullet]
    \item two-pass SS (Transformer, more parallel)
    \item 3D Pareto: $(\downarrow\,\mathrm{Score},\ \uparrow Len_{\text{out}},\ \downarrow Len_{\text{in}})$
    \item alternatives to reduce exposure bias
  \end{itemize}
  }
  \end{tcolorbox}

\end{columns}
\end{frame}